{
  "name": "mizutamari-app",
  "version": "0.1.0",
  "private": true,
  "description": "Water puddle map with separated frontend and backend development surfaces.",
  "scripts": {
    "dev": "node backend/server.js",
    "start": "node backend/server.js",
    "check": "node --check backend/server.js && node --check backend/puddle-service.js && node --check frontend/app.js && node --check frontend/shared/*.mjs && node --check frontend/features/*/*.mjs",
    "test": "node --test backend/*.test.js frontend/features/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
